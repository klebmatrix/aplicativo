services:
  - type: web
    name: aplicativo-quantico
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m streamlit run app.py
    envVars:
      - key: CHAVE_MESTRA
        sync: false # Isso indica que você vai preencher o valor manualmente no painel para segurança
      - key: PYTHON_VERSION
        value: 3.11.0